{
    "name": "htmx",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "npx serve src -p 4003",
        "lint": "eslint /src",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@repo/eslint-config": "*",
        "@repo/typescript-config": "*"
    }
}
